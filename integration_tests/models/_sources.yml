version: 2

sources:
  - name: claims_cdm
    database: "{{ var('claims_preprocessing_input_database','tuva') }}"
    schema:  "{{ var('claims_preprocessing_input_schema','claims_cdm') }}"
    tables:
      - name: medical_claim
      - name: eligibility
